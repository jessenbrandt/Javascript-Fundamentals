// Parameters

function pet(animal) { //declare the variable in the ()
    // let animal = 'cat'
    console.log(`I have a ${animal} as a pet.`);
}
pet('cat');

function name(first, last)
 {
    let name = first + last;
    console.log(`Hello, my name is ${first, last}.`)
}
name('Jessica', 'Brandt');